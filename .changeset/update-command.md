---
"spanical": minor
---

Adds `spanical update`: checks GitHub Releases for a newer version, verifies the download against the release's SHA256SUMS, probe-runs it, and atomically replaces the running binary.
