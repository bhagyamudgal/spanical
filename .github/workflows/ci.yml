name: CI

on:
    push:
        branches: [main]
    # Stacked pull requests target the branch below them rather than main, so
    # limiting this to main would let every pull request but the bottom of a
    # stack merge ungated.
    pull_request:
        branches: [main, "bhagya/**", "feature/**"]

permissions:
    contents: read

jobs:
    check:
        name: Lint, Format & Type Check
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
              with:
                  persist-credentials: false
            - uses: oven-sh/setup-bun@v2
              with:
                  bun-version-file: package.json
            - run: bun install --frozen-lockfile
            - run: bun run lint
            - run: bun run format:check
            - run: bun run typecheck
            - run: bun run test
